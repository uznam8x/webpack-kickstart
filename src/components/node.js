import attrs from '../utils/attrs';
/* export default (args, el) => {
  const group = el.append('g');
  attrs.set({ class: 'node' }, group);

  const rect = group.append('rect');
  attrs.set({ width: 100, height: 60, rx: 8, ry: 8 }, rect);
};
 */

export default {
  add: () => {},
  remove: () => {},
  update: () => {},
};
